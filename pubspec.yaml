name: dealer_caryanam
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  riverpod_annotation:
  flutter_riverpod:
  http:
  shared_preferences:
  jwt_decoder:
  google_fonts:
  email_validator:
  socket_io_client: ^3.0.2
  intl: ^0.20.2
  carousel_slider: ^5.0.0
  number_paginator: ^0.4.3
  fluttertoast: ^8.2.10
  percent_indicator: ^4.2.4
  file_picker: ^8.1.7
  camera: ^0.11.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - 'assets/logo_img/cartechlogo2.png'
    - 'assets/images/'
